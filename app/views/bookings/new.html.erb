<div class="text-center">
  <%= simple_form_for([@cow, @booking]) do |f| %>
    <%= f.input :start_date %>
    <%= f.input :end_date %>
    <div class="form-actions">
      <%= f.button :submit, "Book a cow", class: "text-black" %>
    </div>
  <% end %>
</div>
